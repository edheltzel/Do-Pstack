{
  "name": "pstack",
  "version": "0.15.0",
  "description": "pstack plugin for omp/Pi and Claude Code: sticky /poteto-mode, skills, agents.",
  "private": true,
  "type": "module",
  "bin": {
    "pstack": "./scripts/pstack.mjs"
  },
  "scripts": {
    "test": "vitest run",
    "sync": "node ./scripts/pstack.mjs sync"
  },
  "omp": {
    "extensions": [
      "./extensions/pstack.ts"
    ]
  },
  "pi": {
    "extensions": [
      "./extensions/pstack.ts"
    ]
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
