---
description: Enable poteto-mode for this turn. A working mode, not a one-shot skill. Usage: /poteto-mode [task]
---

This is poteto-mode (a working mode). Read `skill://poteto-mode` (`skills/poteto-mode/SKILL.md`) in full before any work, including the Principles index. Match a playbook if one fits. Copy its steps. Name only principles that changed a decision. Write the reply clean as you draft it.

Then do the assigned work:

$ARGUMENTS
